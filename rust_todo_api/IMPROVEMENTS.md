# Improvements / Next Features

Use this as a living checklist of ideas to add later.

- Update + delete todos (full CRUD) - done
- Input validation (email format, password length, empty title checks) - done
- Better error handling (custom error type + JSON errors) - done
- Migrations with `sqlx::migrate!` - done
- Refresh tokens (access token + refresh token flow)
- Pagination + filters for `/todos`
- Role-based access (admin/user)
- Tests (unit + integration)
